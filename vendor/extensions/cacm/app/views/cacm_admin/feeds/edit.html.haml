%h1 Edit Feed

- form_for :feed, @feed, :url => admin_feed_path(@feed), :html => { :method => :put, :name => "edit_feed_form", :id => "edit_feed_form"} do |f|

  .form-area
    = render :partial => 'form', :locals => {:f => f}
    
  %p.buttons
    = submit_tag 'Update'
    or
    = link_to 'Cancel', admin_feeds_path
