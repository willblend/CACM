%h1 Edit Widget (Safe)
%h2= @widget.name.titleize

- error_messages_for :widget

- form_for :widget, @widget, :url => admin_widget_path(@widget),	:html => { :method => :put, :class => "CACMForm" } do
  %table.fieldset
    %col{ :width => '20%' }
    %col{ :width => '80%' }

    %tbody
      %tr
        %th.label Widget Title
        %td.field
          = text_field :widget, :name
      %tr
        %th.label Widget Content
        %td.field
          = text_area :widget, :content, :cols => '80', :rows => '12', :class => 'widget-editor'
      %tr
        %th.label
        %td.field
          = submit_tag "Save Changes"